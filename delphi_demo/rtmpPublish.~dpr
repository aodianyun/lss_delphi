program Project2;

uses
  ExceptionLog,
  Forms,
  publish in 'rtmp_d\publish.pas' {Form1},
  typdef in 'rtmp_d\typdef.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
